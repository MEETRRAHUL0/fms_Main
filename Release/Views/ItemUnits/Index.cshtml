@model IEnumerable<FMS.tbl_ItemUnits>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MeasuringUnits)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MeasuringUnits)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MeasurintUnitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MeasurintUnitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MeasurintUnitID })
        </td>
    </tr>
}

</table>
